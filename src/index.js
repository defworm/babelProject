const multiply = (a,b) => a * b
const firstNum = 2
const secondNum = 3
const thirdNum =4
let product = multiply(firstNum, secondNum, thirdNum)
console.log(`The product of ${firstNum} and ${secondNum} and ${thirdNum} is ${product}`)
